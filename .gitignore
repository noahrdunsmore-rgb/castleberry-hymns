supabase-credentials.txt
.env
*.env

# eChoice application files and raw assets (only built .pptx files belong in git)
hymn-slides/eChoice/
hymn-slides/Templates/
hymn-slides/Application Folder/
hymn-slides/Sample Songs_ppt/

# Raw per-song slide source (PNGs/PDFs); the builder reads these locally for
# verse selection, but only the built .pptx decks belong in git
hymn-slides/HFWR/
hymn-slides/HFWS/

# Assembled weekly service decks (regenerated from a lineup; not source)
service-decks/
# Pasted worship-order text used only as parser input
order*.txt

__pycache__/

# Local-only secrets / runtime artifacts for the deck builder
email_config.json
saturday_build.log
app.log
